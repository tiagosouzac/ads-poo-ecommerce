services:
  database:
    container_name: ads-poo-ecommerce-database
    image: postgres:17.2-alpine3.21
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: ecommerce
    ports:
      - "5432:5432"
